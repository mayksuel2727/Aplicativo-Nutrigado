<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.appnutrigado">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.Menu"></activity>
        <activity android:name=".view.ListFazendas" />
        <activity android:name=".view.Cadastro_Fazendas" />
        <activity android:name=".view.Cadastro_Animais" />
        <activity android:name=".view.Cadastro_Usuario" />
        <activity android:name=".view.Atualizar" />
        <activity android:name=".view.Animais"></activity>
        <activity android:name=".view.Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>